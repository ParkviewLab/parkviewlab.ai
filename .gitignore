# macOS
.DS_Store

# Secrets — never commit. (Guard against the kinds of files that must not leak.)
*recovery_codes*
*.pem
*.key
*.p12
.env
.env.*
*credentials*
*secret*
*.token

# Editor / OS cruft
*.swp
Thumbs.db

# Build output — generated by build.py / the deploy, never committed (see website.md)
releases/index.html
/_site/
